% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WeightModel.R
\name{getWeightedCounts}
\alias{getWeightedCounts}
\title{Count (Weighted) Fragments in Peak Regions}
\usage{
getWeightedCounts(
  files,
  atacFrag,
  ranges,
  genome,
  species,
  fragWeight = FALSE,
  peakWeight = FALSE,
  resize = FALSE,
  width = 300,
  nWidthBins = 30,
  nGCBins = 10,
  cuts = c(0, 120, 300, 500),
  minFrag = 30,
  maxFrag = 3000,
  smooth = FALSE,
  aRange = 1.5,
  coerceToGenome = TRUE,
  ...
)
}
\arguments{
\item{files}{A character vector of paths to fragment files (e.g., BED or TSV).}

\item{atacFrag}{A \code{list} of \code{data.table} objects or a \code{GRangesList}.}

\item{ranges}{A \code{GRanges} or \code{data.table} object containing peak regions.}

\item{genome}{A \code{BSgenome} object or string (e.g., "hg38") for GC content.}

\item{species}{Character; species name (e.g., "human") for chromosome filtering.}

\item{fragWeight}{Logical; whether to apply fragment-level bias correction.}

\item{peakWeight}{Logical; whether to apply cyclic loess normalization on counts.}

\item{resize}{Logical; whether to resize peak ranges to a fixed width.}

\item{width}{Integer; width to resize peaks to if \code{resize=TRUE}.}

\item{nWidthBins, nGCBins}{Integer; number of bins for fragment weighting.}

\item{cuts}{Numeric vector; fragment length thresholds for classification.}

\item{minFrag, maxFrag}{Integer; fragment length filters.}

\item{smooth}{Logical; whether to apply smoothing on fragment weights.}

\item{aRange}{Numeric; bandwidth for smoothing.}

\item{...}{Additional arguments passed to internal weighting functions.}
}
\value{
A \code{\link[SummarizedExperiment]{SummarizedExperiment}} object
containing assays for different fragment types.
}
\description{
Count the number of fragments in each peak region, with or without
fragment-level and peak-level weighting (normalization).
}
\author{
Jiayi Wang
}
